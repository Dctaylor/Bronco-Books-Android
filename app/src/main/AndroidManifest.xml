<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.broncobooks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ManualSellActivity"
            android:label="@string/title_activity_manual_sell"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".TestingActivity">
        </activity>

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ListingActivity" />
        <activity
            android:name=".TestingActivity"
            android:label="@string/title_activity_testing"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>